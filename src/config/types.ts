const REPOSITORIES = {
    AccountRepository: Symbol.for('AccountRepository'),
};

const USECASES = {
    AuthUsecases: Symbol.for('AuthUsecases'),
};

const DOMAIN_SERVICES = {
    AccountService: Symbol.for('AccountService'),
};

export { REPOSITORIES, USECASES, DOMAIN_SERVICES };
